# JRA API 進捗管理

最終更新: 2026-06-28

## 全体ステータス

- 完了
  - JRADB + cname 導線への統一
  - meeting ベースの出馬表 / オッズ / 結果取得
  - `wide` `exacta` `trio` を含む券種拡張
  - MCP HTTP 入口の追加
  - batch の JSONL 保存
  - 生成物の `.gitignore` 整理
  - API 例外の HTTP レスポンス化
    - `400`: unsupported bet type などの不正入力
    - `404`: race / meeting 未発見
    - `502`: upstream 取得失敗
  - provider retry / backoff
    - timeout / request error / 5xx を共通 retry
    - 4xx は即時失敗
    - GET / POST 共通化
  - 文字コードと文字化けの整理
    - README の文字化けを修正
    - OpenAPI 文言を UTF-8 に修正
    - 管理 docs を UTF-8 で再整理
    - OpenAPI テストの期待値を更新

- 進行中
  - なし

- 未着手
  - 優先順位 B 以降

## 直近の実装対象

### A-1 API 例外の HTTP レスポンス化

- 状態: 完了
- 目的:
  - 利用者から見て `500` を減らす
  - エラー原因を `400 / 404 / 502` に切り分ける
- 実装結果:
  - `JraApiError` 系例外を追加
  - FastAPI 例外ハンドラを追加
  - service 内の主要 `LookupError` を `400 / 404` 向け例外へ置換
  - API テストで `404 / 400 / 502` を追加

### A-2 provider retry / backoff

- 状態: 完了
- 目的:
  - JRA 側の一時失敗で即座に API 全体を落とさない
  - GET / POST で同じ retry 方針を使う
- 実装結果:
  - `HttpProvider` に共通 retry ヘルパを追加
  - retry 対象を `timeout / request error / 5xx` に限定
  - `4xx` は retry せず即時失敗
  - provider テストを追加

### A-3 文字コードと文字化けの整理

- 状態: 完了
- 目的:
  - README、OpenAPI、管理 docs の可読性を回復する
- 実装結果:
  - `README.md` の文字化けを修正
  - `src/jra_srb/app.py` の OpenAPI 文言を UTF-8 に修正
  - `docs/jra/08_実装タスクと優先順位.md` を UTF-8 で再整理
  - `docs/jra/09_進捗管理.md` を UTF-8 で再整理
  - OpenAPI テストの期待値を更新

## 次の更新ルール

- タスクを開始したら「進行中」へ移動
- テスト完了後に「完了」へ移動
- ブロッカーが出た場合はここに原因を追記
