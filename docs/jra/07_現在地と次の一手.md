# 現在地と次の一手

## 現在地

ここまでで、設計書と実装計画に対して次は完了済みである。

- JRA navigation 層
- meeting 一覧 API
- 開催地座標からの出馬表取得
- 開催地座標からのオッズ取得
- 開催地座標からの結果・払戻取得
- 過去結果バッチの最小骨組み
- テスト整備

## 現在のギャップ

- JRA 実オッズの券種拡張
- batch の保存・retry・skip 制御
- formal docs は今回作成したが、今後は feature ごとに同期運用へ移す必要がある

## 次の一手

最も自然なのは次のどちらかである。

1. odds 券種拡張
- `quinella`
- `exacta`
- `wide`
- `trio`

2. batch の実用化
- 保存先 abstraction
- result / payout の永続化
- retry
- skip 制御

現状のコード資産を活かすなら、先に odds 券種拡張を行う方が小さな縦切りとして進めやすい。
