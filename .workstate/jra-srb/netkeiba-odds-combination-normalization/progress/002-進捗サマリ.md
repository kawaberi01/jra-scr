# 進捗サマリ

## 2026-07-02

- `NetkeibaService._filter_odds` の combination 比較を券種別正規化に変更した。
- `UNORDERED_NETKEIBA_BET_TYPES` を追加し、`bracket_quinella` / `quinella` / `wide` / `trio` は比較時だけ順不同にした。
- `exacta` / `trifecta` は順序を維持したまま比較する仕様にした。
- query 側と entry 側の両方に `05 -> 5` の数字文字列正規化を適用するようにした。
- netkeiba odds API テストに wide / quinella / trio の逆順一致、exacta / trifecta の順序維持ケースを追加した。
