# 002-進捗サマリ

## 2026-06-28

### 実装済み

- 結果収集 Job API を追加。
  - `POST /jobs/result-collections`
  - `GET /jobs/result-collections`
  - `GET /jobs/result-collections/{job_id}`
- in-memory job registry を `src/jra_srb/jobs.py` に追加。
- job status を `queued`, `running`, `succeeded`, `failed` で管理。
- job 実行は既存 `PastResultCollector` と JSONL / SQLite storage を再利用。
- レース検索 API を追加。
  - `GET /search/races`
- `models.py` に Job API / Search API の request / response DTO を追加。
- `/stored/results` と Job API の storage 選択規則を共通 helper に整理。
- README と `docs/jra/05_API仕様.md` に利用例と API 契約を追記。
- API テストを追加。

### 検証結果

- `uv run --extra dev pytest tests/test_api.py -q`
  - `21 passed, 1 warning`
- `uv run --extra dev pytest -q`
  - `65 passed, 1 skipped, 1 warning`

### 残件

- Job registry の永続化は未実装。
- Job cancel / retry endpoint は未実装。
- job 同時実行数制限は未実装。
- API key / CORS は未実装。

## 2026-06-28 実データ取得確認

### 実施内容

- JRA upstream から `2026-06-28` 開催分のレース一覧を取得。
- 対象開催場:
  - `fukushima`
  - `kokura`
  - `hakodate`
- レース別に結果取得を試行。

### 保存先

- `data/live-2026-06-28-races.json`
- `data/live-2026-06-28-results-by-race.json`
- `data/live-2026-06-28-results.jsonl`

### 結果

- レース一覧: 36 件取得成功。
- 結果データ: 36 件中 23 件取得成功、13 件失敗。
- 失敗理由は後半レースの `payout block not found`。JRA 側で払戻ブロックが未掲載、または取得時点で結果未確定の可能性がある。

### 備考

- `data/` はローカル実行成果物のため `.gitignore` に追加し、commit 対象外にする。
