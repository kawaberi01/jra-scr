# 005-検証メモ

## 2026-06-28

### 実行コマンド

```powershell
$env:UV_PROJECT_ENVIRONMENT='.venv-win'; uv run --extra dev pytest tests/test_api.py -q
```

結果:

- `21 passed, 1 warning`

```powershell
$env:UV_PROJECT_ENVIRONMENT='.venv-win'; uv run --extra dev pytest -q
```

結果:

- `65 passed, 1 skipped, 1 warning`

### 注意

- warning は Pydantic 2.11 系の deprecation warning。今回追加したテスト失敗ではない。
- テストは fake service / fixture provider を使い、実 upstream には依存していない。
