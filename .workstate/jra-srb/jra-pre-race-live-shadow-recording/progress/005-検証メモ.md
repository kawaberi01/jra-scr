# 005-検証メモ

更新方式: 追記型。

## 2026-07-17 分析記録

### 1. 実施内容

- 実コードの入口、保存、判断policy、model artifactを静的確認。

### 2. 結果

- 実装に必要なデータはScout成功経路で取得可能。

### 3. 既知失敗

- なし。

### 4. 次の確認点

- 実装後に対象test、全pytest、ruffを実行する。

## 2026-07-17 実装検証

### 1. 実施内容

- `rtk uv run pytest -q tests/test_jra_day_race_scout.py tests/test_analysis_store.py tests/test_api.py`
- `rtk uv run pytest -q`
- `rtk uv run ruff check .`
- `rtk git diff --check`

### 2. 結果

- 対象test: `86 passed`。
- 全体pytest: `305 passed, 1 skipped`。
- ruff: `All checks passed!`
- diff whitespace error: なし。

### 3. 既知失敗・警告

- Pydantic 2.11の既存非推奨警告が1件。
- `ruff format --check`は既存7ファイル全体を未整形と判定したため、自動整形による巨大な無関係差分は作成していない。

### 4. 次の確認点

- 実運用のrace-scout実行で観測を蓄積し、別runの時系列が参照できること。
