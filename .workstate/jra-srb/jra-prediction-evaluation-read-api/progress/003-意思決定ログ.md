# 003-意思決定ログ

更新方式: 追記型。

## 2026-07-17 仕様化セッション

### 種別

- 分析・仕様化

### 入力

- 主資料: ユーザー要件、前回のAPI/CLI俯瞰結果
- 補助資料: app.py、analysis_store.py、models.py、既存テスト、API仕様書

### 今回の変更内容

- 最優先機能を「JRA予想・評価参照API」と定義した。
- 詳細2本、一覧2本、評価集計1本のGET APIに範囲を固定した。

### 採用判断

- 既存DB schemaと作成・評価ロジックは変更しない。
- responseはDB列名ではなく、JSON objectとして意味のある公開名を使う。
- 一覧は既存Page形式と最大500件を流用する。
- summaryは既存評価列だけで算出可能な指標に限定する。

### 変更しなかった内容

- DB index、POST型付け、Nankan共通化、MCP公開、スクリプト置換。

### 判断根拠

- 実装範囲を1機能に保ち、既存コードパターンへ局所追加できるため。

### 影響範囲

- models、store、FastAPI route、対象テスト、API仕様書。

### 残件

- 次フェーズで実装する。

## 2026-07-17 11:33 実装セッション

### 種別

- 実装

### 入力

- 主資料: `030-JRA予想評価参照API実装指示書.md`、`020-JRA予想評価参照API実装計画書.md`
- 補助資料: 対象ソースの限定diff、既存Store/API/テストパターン

### 今回の変更内容

- 参照用model、Storeの詳細・一覧・集計method、GET API 5本、対象テスト、API仕様を追加した。

### 採用判断

- 一覧のID取得と詳細復元を同一SQLite接続内で行い、既存ページ形式を維持した。
- 評価集計のnullable指標は`count(column)`を分母とし、未評価値を除外した。
- `max_single_payout`は既存検証ロジックと名称に合わせ、評価合計ではなく券単位の最大払戻とした。

### 変更しなかった内容

- DB schema、既存POST、MCP operation、予想生成・評価計算、既存差分。

### 判断根拠

- `030/020`の実装契約と既存`AnalysisSQLiteStore`、`BetRecordPage`、共通例外handlerのパターン。

### 影響範囲

- models、analysis store、FastAPI route、対象テスト、API仕様書。

### 残件

- 人間による対象pytest実行。
