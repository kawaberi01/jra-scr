# 006-次回着手メモ

## 次回候補

1. deterministic evaluator
   - `prediction_tickets` と `payouts` を照合し、`evaluations` / `evaluation_ticket_results` に保存する。
2. Prediction JSON schema validation
   - `docs/nakaana_self_learning_agent_spec.md` の予想JSONを Pydantic model 化する。
3. CSV / Parquet export
   - `analysis.sqlite` からデータレイク向けに出力する。
4. `/analysis/*` API
   - `pre-race-snapshot`, result, payouts, backtest summary をAPI化する。
5. Job API 化
   - `collect-analysis` を HTTP job として実行できるようにする。
6. 取り込み品質チェックのCLI化
   - 最新 run の `collection_errors`、孤立騎手記号、主要テーブル件数を確認する検査を
     `.workstate` スクリプトではなく正式コマンドに寄せる。

## 再開時の確認

- `src/jra_srb/analysis_store.py`
- `src/jra_srb/analysis_collector.py`
- `src/jra_srb/service.py`
- `tests/test_analysis_store.py`
- `tests/test_analysis_collector.py`
- `tests/test_service_result_enrichment.py`

## 2026-07-01 完了後の状態

- 2025 年の `runners` は `data/analysis.sqlite` に投入済み。
- `runners_2025=47884`
- `races_with_runners_2025=3455`
- `result_runner_join_rows_2025=47884`
- `payout_runner_race_join_rows_2025=574045`
- 最終投入 run_id: `572d9762-522b-4277-97a0-83d09388de04`
- 最終投入 run の `collection_errors=0`

## 次回注意

- historical card は、JRA の過去出馬表 CNAME が取得できない日では result ページ表から
  `RaceCard.runners` 相当を構築する fallback に依存する。
- card odds / popularity は result ページ由来 fallback では取得できないため null のまま。

## 2026-07-01 正式 CLI 化後の再開メモ

- runners backfill は `jra-srb backfill-analysis-runners` で再実行可能。
- join 検証は `jra-srb verify-analysis-joins` で実行可能。
- 2025 年分は `verify-analysis-joins --db data/analysis.sqlite --from-date 2025-01-01 --to-date 2025-12-31 --sample-size 5` が exit code 0。
- `.workstate` の `fill_2025_runners_from_cards.py` / `verify_2025_card_result_join.py` 相当は正式 CLI に移行済み。既存スクリプトは未削除。
- 次回以降の残件は deterministic evaluator、Prediction schema、export、API/Job 化。
