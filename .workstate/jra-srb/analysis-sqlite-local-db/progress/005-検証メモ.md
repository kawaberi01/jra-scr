# 005-検証メモ

## 2026-06-28

### 実行コマンド

```powershell
$env:UV_PROJECT_ENVIRONMENT='.venv-win'; uv run --extra dev pytest tests/test_analysis_store.py tests/test_analysis_collector.py -q
```

結果:

- `8 passed`

```powershell
$env:UV_PROJECT_ENVIRONMENT='.venv-win'; uv run --extra dev pytest tests/test_analysis_store.py tests/test_analysis_collector.py tests/test_cli.py -q
```

結果:

- `9 passed`

```powershell
$env:UV_PROJECT_ENVIRONMENT='.venv-win'; uv run --extra dev pytest -q
```

結果:

- `73 passed, 1 skipped, 1 warning`

### 注意

- warning は既存の Pydantic deprecation warning。
- 新規テストは fake service を使い、実 upstream に依存していない。

## 2026-06-30

### 実行コマンド

```powershell
cmd /c "set UV_PROJECT_ENVIRONMENT=.venv-win&& uv run --extra dev pytest tests/test_service.py tests/test_service_result_enrichment.py tests/test_extractors.py -q"
```

結果:

- `17 passed`

### 確認内容

- 会場一覧型の結果取得 (`get_race_result_by_number`) で、結果ページに騎手列がない場合でも
  出走表の `horse_no` 対応から騎手名が補完されることを確認。
- 既存の `test_extractors.py` と `test_service.py` を同時に通し、直近の騎手名補正と
  結果取得系の回帰が出ていないことを確認。

## 2026-07-01

### 実行コマンド

```powershell
cmd /c "set UV_PROJECT_ENVIRONMENT=.venv-win&& uv run --extra dev pytest tests/test_service_historical_meeting_fallback.py tests/test_extractors.py tests/test_service_result_enrichment.py -q"
```

結果:

- `6 passed`

```powershell
cmd /c "set UV_PROJECT_ENVIRONMENT=.venv-win&& uv run --extra dev pytest -q"
```

結果:

- `81 passed, 1 skipped, 1 warning`

### 実 upstream 確認

```powershell
cmd /c "set UV_PROJECT_ENVIRONMENT=.venv-win&& set PYTHONIOENCODING=utf-8&& uv run python .workstate\jra-srb\analysis-sqlite-local-db\progress\inspect_2025_card_path.py"
```

結果:

- `2025-01-05 nakayama 11R` で result fallback により `runners=18`
- サンプル: `アルナシーム`, `藤岡 佑介`, `橋口 慎介`

### DB 投入・結合確認

```powershell
cmd /c "set UV_PROJECT_ENVIRONMENT=.venv-win&& set PYTHONIOENCODING=utf-8&& uv run python .workstate\jra-srb\analysis-sqlite-local-db\progress\fill_2025_runners_from_cards.py"
```

結果:

- run_id: `572d9762-522b-4277-97a0-83d09388de04`
- status: `succeeded`
- targets: `3455`

```powershell
cmd /c "set UV_PROJECT_ENVIRONMENT=.venv-win&& set PYTHONIOENCODING=utf-8&& uv run python .workstate\jra-srb\analysis-sqlite-local-db\progress\verify_2025_card_result_join.py"
```

結果:

- `races_2025=3455`
- `race_results_2025=3455`
- `result_entries_2025=47884`
- `payouts_2025=41257`
- `runners_2025=47884`
- `races_with_runners_2025=3455`
- `result_runner_join_rows_2025=47884`
- `payout_runner_race_join_rows_2025=574045`
- `latest_run_errors=0`

サンプル確認:

- `202501050601`: `ピッツァナポリ` / `大野 拓弥` / `新開 幸一`
- `202501050602`: `フィーリウス` / `木幡 巧也` / `手塚 貴久`
- `202501050603`: `ホウオウサムレット` / `T.マーカンド` / `田中 博康`

## 2026-07-01 正式 CLI 化後の検証

### 実行コマンド

```powershell
$env:UV_PROJECT_ENVIRONMENT = Join-Path $env:TEMP "jra-scr-uv-test-venv"; uv run --extra dev --python 3.12 pytest -q tests/test_analysis_maintenance.py tests/test_cli.py tests/test_analysis_collector.py
```

結果:

- `12 passed`

```powershell
$env:UV_PROJECT_ENVIRONMENT = Join-Path $env:TEMP "jra-scr-uv-test-venv"; uv run --extra dev --python 3.12 jra-srb verify-analysis-joins --db data/analysis.sqlite --from-date 2025-01-01 --to-date 2025-12-31 --sample-size 5
```

結果:

- exit code: `0`
- `races=3455`
- `runners=47884`
- `race_results=3455`
- `result_entries=47884`
- `payouts=41257`
- `races_with_runners=3455`
- `result_runner_join_rows=47884`
- `payout_runner_race_join_rows=574045`
- `missing_runner_races=0`
- `latest_run_errors=[]`

```powershell
$env:UV_PROJECT_ENVIRONMENT = Join-Path $env:TEMP "jra-scr-uv-test-venv"; uv run --extra dev --python 3.12 pytest -q
```

結果:

- `87 passed, 1 skipped, 1 warning`

### 注意

- `.venv` は Python 実体がない壊れた venv と判定されたため触らず、`%TEMP%\jra-scr-uv-test-venv` を使って検証した。
- warning は既存の Pydantic deprecation warning。

## 2026-07-01 追加監査後の検証

### 実行コマンド

```powershell
$env:UV_PROJECT_ENVIRONMENT = Join-Path $env:TEMP "jra-scr-uv-test-venv"; uv run --extra dev --python 3.12 pytest -q tests/test_analysis_maintenance.py tests/test_cli.py tests/test_analysis_collector.py
```

結果:

- `15 passed`
- dry-run が service を呼ばず、`runners` / `collection_runs` に書き込まないことを確認。

```powershell
$env:UV_PROJECT_ENVIRONMENT = Join-Path $env:TEMP "jra-scr-uv-test-venv"; uv run --extra dev --python 3.12 pytest -q
```

結果:

- `90 passed, 1 skipped, 1 warning`

```powershell
$env:UV_PROJECT_ENVIRONMENT = Join-Path $env:TEMP "jra-scr-uv-test-venv"; uv run --extra dev --python 3.12 jra-srb verify-analysis-joins --db data/analysis.sqlite --from-date 2025-01-01 --to-date 2025-12-31 --sample-size 5
```

結果:

- exit code: `0`
- `races=3455`
- `runners=47884`
- `race_results=3455`
- `result_entries=47884`
- `payouts=41257`
- `races_with_runners=3455`
- `result_runner_join_rows=47884`
- `payout_runner_race_join_rows=574045`
- `missing_runner_races=0`
- `latest_run_errors=[]`
- sample は `horse_no / horse_name / jockey / trainer / sex_age / weight_carried` が出力されることを確認。
