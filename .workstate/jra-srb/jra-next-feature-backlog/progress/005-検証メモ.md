# 005-検証メモ

更新方式: 追記型。検証実行ごとに日付付きセクションを追記し、既存結果は削除しない。

## 2026-07-17 バックログ初期確認

### 1. 実施内容

- 既存Featureの次回着手メモ、進捗サマリ、保留事項、実装計画を横断確認した。
- 現行コードを検索し、実装済み候補と未実装候補を切り分けた。

### 2. 結果

- 追加候補を18件へ統合した。
- 基本Job API、予想・評価参照API、snapshot/timeline参照API、deterministic evaluatorは実装済みとして除外した。
- 直近の全体検証結果は`pytest: 300 passed, 1 skipped`、`ruff check .: All checks passed`。

### 3. 既知失敗

- なし。今回は進捗文書のみ作成し、ソースコードは変更していない。

### 4. 次の確認点

- 最初に着手するP0タスクを確定する。
- 着手時に現行コードと個別仕様のgroundingを改めて実施する。

## 2026-07-17 JRA-NEXT-001検証

### 1. 実施内容

- 対象pytest、全pytest、全ruff check、diff check。

### 2. 結果

- 対象test: `86 passed`。
- 全体pytest: `305 passed, 1 skipped`。
- `ruff check .`: `All checks passed!`
- `git diff --check`: 問題なし。

### 3. 既知失敗

- なし。既知のPydantic非推奨警告が1件。

### 4. 次の確認点

- 実運用で観測を蓄積後、確定結果との評価フローを別タスクで確認する。

## 2026-07-17 JRA-NEXT-002検証

### 1. 実施内容

- 旧schema migration、追記保存、snapshot/timeline参照、collector/CLI test。
- 全pytest、ruff、diff check。

### 2. 結果

- 対象test: `60 passed`。
- 全体pytest: `309 passed, 1 skipped`。
- `ruff check .`: 成功。

### 3. 既知失敗

- なし。既知のPydantic非推奨警告が1件。

### 4. 次の確認点

- 大容量実DBの初回migration時間は運用時に確認する。

## 2026-07-17 JRA-NEXT-003検証

### 1. 実施内容

- 対象pytest、全pytest、ruff、compileall、diff check。

### 2. 結果

- 対象pytest: `114 passed`。
- 全pytest: `314 passed, 1 skipped`。
- ruff: `All checks passed`。
- compileall、`uv build`、diff check: 成功。

### 3. 既知失敗

- なし。既知のPydantic非推奨警告が1件。

### 4. 次の確認点

- 未知のJRA取消markupが観測された場合、fixtureを追加して明示判定を拡張する。
