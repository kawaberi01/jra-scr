# 006-次回着手メモ

更新方式: 更新型。次回の最初の1手だけを最新化し、古い次回メモは残さない。

- 次回の最初の1手: `JRA-NEXT-005` Job cancel・retry APIの現行状態と許可する状態遷移を調査する。
- その前提: JRA-NEXT-001〜004は仕様化、実装、自動検証まで完了。Job registry は `JRA_SRB_JOBS_PATH`（既定 `data/jobs.sqlite`）へ保存される。
- 注意点: cancel/retry は SQLite 永続状態を唯一の正とし、二重実行や再起動時の自動再実行を導入しない。
