# 003-意思決定ログ

更新方式: 追記型。判断が発生した単位で日付付きセクションを追記し、既存判断は削除しない。

## 2026-07-17 バックログ初期化

### 種別

- 分析 / 進捗整理

### 入力

- `analysis-sqlite-local-db`
- `additional-api-features`
- `jra-prediction-evaluation-read-api`
- `jra-pre-race-snapshot-odds-timeline-read-api`
- `jra-win-ev-revalidation`
- `jra-ticket-safety`
- `jra-prediction-materials-api`

### 今回の変更内容

- 複数Featureに分散していた追加候補を18件の共通バックログへ統合した。
- 実装済みの参照API、deterministic evaluator、基本Job API、API品質改善は候補から除外した。

### 採用判断

- P0はライブシャドー記録、append-only snapshot、as-of出馬表、Job永続化とする。
- セキュリティとMCP公開は利用条件付きとする。
- 性能indexとモデル再校正は根拠となる計測・新規データが揃うまでblockedとする。

### 変更しなかった内容

- 現行の`shadow_only`購入抑止方針。
- 既存API、DB、collectorの実装。
- 各Feature配下に残る過去の進捗記録。

### 判断根拠

- 追加機能の進捗確認場所を一つに固定するため。
- 古い候補には実装済み項目が混在しており、そのままでは残件を誤認するため。

### 影響範囲

- `.workstate/jra-srb/jra-next-feature-backlog/progress/`のみ。

### 残件

- P0内の実装順を確定する。
- 着手タスクごとに個別Featureの`000/005/010/020/030`を作成する。

## 2026-07-17 JRA-NEXT-001完了

### 種別

- 仕様化 / 実装 / 検証

### 今回の変更内容

- race-scout成功時に候補、単勝オッズ、モデル版、shadow判断を同一観測として保存する機能を追加した。
- race単位の時系列参照APIを追加した。

### 採用判断

- 観測tableを既存odds snapshotから独立させ、別runをappend保存する。
- 全保存判断から購入ticketを除去し、`shadow_only`を保証する。

### 影響範囲

- `models.py`、`jra_day_race_scout.py`、`analysis_store.py`、`app.py`、関連test。

### 残件

- 次は`JRA-NEXT-002`のappend-only odds snapshotを仕様化する。

## 2026-07-17 JRA-NEXT-002完了

### 種別

- 仕様化 / 実装 / 検証

### 今回の変更内容

- odds snapshotを世代ごとの追記保存へ変更した。
- 旧unique schemaの無損失migration、参照互換、明示的な再取得optionを追加した。

### 採用判断

- timelineは全世代、pre-race snapshotは最新1世代/券種を返す。
- 定刻collectorの既定skipを維持し、`--refresh-existing`でだけ再取得する。

### 残件

- 次は`JRA-NEXT-003`のas-of出馬表履歴化を仕様化する。
