# 検証メモ

## 2026-06-28

対象テスト:

```powershell
$env:UV_PROJECT_ENVIRONMENT='.venv-win'
uv run --extra dev pytest -q tests/test_api.py tests/test_batch.py
```

結果:

- 21 passed
- 1 warning

変更対象を含む全体確認:

```powershell
$env:UV_PROJECT_ENVIRONMENT='.venv-win'
uv run --extra dev pytest -q -k 'not auto_progress'
```

結果:

- 57 passed
- 2 deselected
- 1 warning

補足:

- `tests/test_auto_progress.py` は既知の外部スキルパス環境依存のため除外。

外部スキルパス修正後の全体確認:

```powershell
$env:UV_PROJECT_ENVIRONMENT='.venv-win'
uv run --extra dev pytest -q
```

結果:

- 57 passed
- 1 skipped
- 1 warning

補足:

- `tests/test_auto_progress.py` は外部スキル `addon-superpowers-auto-progress` が未導入の環境では skip するように修正。
