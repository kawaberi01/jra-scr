# 進捗サマリ

## 2026-06-28

- `020-api-quality-roadmap実装計画書.md` の P0 を実装対象に固定した。
- 既存 API / storage / README / tests を確認し、エラー標準化、保存済み結果ページング、docs 同期を進める。
- `ApiError` / `ApiErrorResponse` を追加し、独自例外と 422 を `{"error": ...}` 形式に統一した。
- request middleware で `x-request-id` を受け取り、未指定時は UUID を発行してレスポンスヘッダーとエラー本文に入れるようにした。
- `GET /stored/results` に `limit` / `offset` を追加し、`items,total,limit,offset` 形式へ変更した。
- JSONL storage は slice、SQLite storage は SQL の `count`, `limit`, `offset` でページングするようにした。
- README と `docs/jra/05_API仕様.md` に追加 endpoint、エラー形式、CLI、環境変数、Windows/WSL の uv 運用を反映した。
