## 意思決定ログ

### 2026-07-05
- 決定:
  - 地方競馬 netkeiba 追加 API は `/nar/...` 配下で新設する。
- 理由:
  - 既存 `/netkeiba/races/...` は中央 race_id ベース補完 API として意味が固まっている。
  - JRA API と地方 API の利用者認知を分けやすい。

### 2026-07-05
- 決定:
  - NAR 用 provider / service / extractors は新規ファイルとして分離する。
- 理由:
  - 既存実装の責務分離に沿う。
  - `nar.netkeiba.com` と `race.sp.netkeiba.com` はホストとページ構造が異なる。

### 2026-07-05
- 決定:
  - 初回スコープは calendar / meeting list / card / result / odds に限定する。
- 理由:
  - 既存要望に直接対応できる。
  - analysis.sqlite 保存や大量巡回を同時に持ち込まないため。

### 2026-07-05
- 決定:
  - odds は HTML `type=` 切り替えを最初の取得元とする。
- 理由:
  - 現時点で JSON API の存在は確認していない。
  - HTML でも初回要件は満たせる。
