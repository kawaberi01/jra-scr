## 進捗サマリ
- 地方競馬 netkeiba API 追加の仕様化を開始した。
- 現行の JRA / netkeiba 補完 API の構成を確認し、NAR 追加先は別モジュール群に分ける方針で整理した。
- `nar.netkeiba.com` の URL 規則、`kaisai_id`、`race_id` の組み立て規則を確認した。
- `000 / 005 / 010 / 020 / 030` の仕様成果物を作成した。

## 現時点の結論
- API 追加は可能。
- ただし初回は fixture first、単一開催日・単一レース取得を優先する。
- `wakuren` / `wakutan` と odds `type=` の完全対応は未確定事項として残す。
