# 030-jra-ticket-safety 実装指示書

## 実装順序

1. `jra_prediction_engine.py` の固定ワイド生成を、取得済みワイドオッズを条件とする処理へ置換する。
2. `prediction_json` に買い目方針を記録する。
3. `app.py` の予想作成bundleで `win,wide` を要求する。
4. 単体テストを追加する。

## 禁止事項

- 履歴モデル・既存の未コミット変更・DBスキーマに触れない。
- オッズがない組合せの配当を推測しない。
- 順位スコアの係数を今回変更しない。
