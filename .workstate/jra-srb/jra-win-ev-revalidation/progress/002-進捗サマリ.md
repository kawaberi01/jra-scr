# 002-進捗サマリ

更新方式: 更新型。

## 現在地

- 通常戦EVを `shadow_only` に安全化し、履歴確定オッズを使った再検証を完了した。
- 外部評価が昇格条件を満たさないため、運用状態は `shadow_only` のままである。

## 完了済み

- 現行EVロジックの用途不整合を整理。
- 未校正確率、大穴偏重、未実施holdoutを仕様へ反映。
- 5段階の再検証と昇格条件を固定。
- `history_win_ev` の購入ticketsを抑止し、候補はシャドー表示だけに変更。
- netkeibaの確定単勝オッズを履歴評価専用の入力として監査。
- 70/15/15固定分割、校正比較、固定グリッド、外部評価、採否成果物を作成。

## 未完 / 注意点

- 対象は2025-01-01〜2026-05-31の通常戦。取消・除外368レースを除外し、3,863レースで結果・払戻・最終単勝オッズの取得率は各100%。
- 外部評価は579レース・224候補。回収率96.34%、単勝30倍超採用率21.43%、候補日数19日のため昇格不可。
- ライブシャドーは外部評価不合格のため未開始。

## テスト状況

- `rtk uv run pytest -q tests/test_jra_betting_decision.py` : 5 passed。
- 監査スクリプト: `ready_for_split`。
- 再検証: `shadow_only`。購入推奨には昇格しない。

## 次にやる候補

- ライブの発走前オッズ・候補を30開催日かつ200候補以上蓄積する（外部評価再合格後のみ）。
- 同じ外部評価期間を使った閾値再調整は禁止。新たな未使用期間を用意しない限り昇格しない。

## 再開時の読み順

- `006-次回着手メモ.md`
- 本ファイル
- `spec/030` → `020` → `010`
