# 002-進捗サマリ

## 現在地

- 実装・対象テスト・差分確認まで完了。

## 完了済み

- 実コードに基づく現行仕様整理。
- `MeetingRace.race_grade`、会場一覧HTMLからの抽出、SQLiteの`races.race_grade`保存を実装。
- `pytest -q tests/test_extractors.py tests/test_analysis_store.py tests/test_api.py`が100件成功。

## 未完 / 注意点

- 過去データの格付けバックフィルは対象外。以後の会場一覧取得時に保存される。

## 次にやる候補

- 未知のgrade icon表記が見つかった場合は、実HTMLを追加して正規化規則を拡張する。
