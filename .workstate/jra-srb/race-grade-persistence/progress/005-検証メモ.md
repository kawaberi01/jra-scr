# 005-検証メモ

## 実行結果

- 実行コマンド: `rtk uv run pytest -q tests/test_extractors.py tests/test_analysis_store.py tests/test_api.py`
- 結果: 100 passed, 1 warning
- 警告: 既存依存のPydantic 2.11非推奨警告。今回の変更による失敗ではない。

## 確認内容

- 会場一覧HTMLの`grade_icon`から`GⅢ`を`G3`へ正規化する。
- OPと格付けなしをそれぞれ`OP`、`NULL`として扱う。
- SQLite初期化で`races.race_grade`を作成し、既存DBには互換的に列を追加する。
- カード詳細のUPSERT後も、先に保存された格付けが保持される。
- APIの会場一覧レスポンスに`race_grade`を含める。

## 差分確認

- `rtk git diff --check`: 成功
