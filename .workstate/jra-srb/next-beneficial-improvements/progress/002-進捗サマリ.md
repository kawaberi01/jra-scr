# 進捗サマリ

## 2026-06-28

- 全タスク実装の依頼を受け、`020` の P0 から P3 までを実装対象に拡張した。
- 実装前の対象 spec と既存コードの入口を確認した。
- `normalization.py` を追加し、日本語開催場・券種・レース番号表記を canonical value へ正規化できるようにした。
- API パラメータに `CourseCode` / `BetType` Enum と `race_id` / `race_no` 制約を追加した。
- `/normalize`, `/stored/results`, `/stored/results/{race_id}`, `/health/upstream` を追加した。
- `collect-results` CLI と `jra-srb` console script を追加した。
- `ResultStorage` に read 契約を追加し、JSONL 読み出しと SQLite backend を追加した。
- `SQLiteTTLCache` を追加し、`JRA_SRB_CACHE_PATH` 指定時に永続 cache を使えるようにした。
- request / provider / service の主要ログを標準 logging で追加した。
