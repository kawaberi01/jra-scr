# 次回着手メモ

## 状態

P0 から P3 までの実装は完了。

## 残る確認

- `tests/test_auto_progress.py` の外部スキルパス解決は今回変更外の環境依存失敗として残っている。
- `.venv` は Windows 側の Python executable を持たないため、Windows では `UV_PROJECT_ENVIRONMENT=.venv-win` を指定して `uv run` する。
- 本番 JRA upstream への疎通確認は未実施。
