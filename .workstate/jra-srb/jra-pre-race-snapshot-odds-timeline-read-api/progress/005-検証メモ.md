# 005-検証メモ

更新方式: 追記型。

## 2026-07-17 仕様化時の静的確認

### 1. 実施内容

- 関連ファイル名と限定キーワード検索。
- DB schema、保存処理、既存snapshot取得、error handler、response model流儀の確認。
- 既存テストによる結果リーク防止契約の確認。

### 2. 結果

- API 2本を既存Store/DI流儀の範囲で追加可能。
- DB schema変更は不要。
- 結果系テーブルを参照せず実装可能。
- `odds_timing`文字列順は時間順に使えないため`fetched_at`順を仕様化した。

### 3. 未実施

- build、test、lint、アプリ起動、DB接続、外部API呼び出し。
- 分析skillの明示制約により実行していない。

### 4. 次の確認点

- 実装後に対象Store/API testとruffを人間が実行する。
- 既存未コミット差分との競合を限定diffで確認する。

## 2026-07-17 14:33 実装後の静的確認

### 1. 実施内容

- 対象Python 5ファイルのAST解析。
- `jra_srb.models`、`jra_srb.analysis_store`、`jra_srb.app`のimport確認。
- 対象6ファイルの限定diff、参照箇所、MCP allowlist、whitespace確認。

### 2. 結果

- AST解析: 成功。
- module import: 成功。
- `git diff --check`: 成功。
- 既存`get_pre_race_snapshot(race_id)`呼び出し箇所は更新済み。
- 新規2 endpointは`MCP_OPERATION_IDS`へ追加していない。

### 3. 既知失敗

- なし。
- pytest/ruffはimplementation skillのコマンド制約により未実行。

### 4. 次の確認点

- `rtk uv run pytest -q "tests\test_analysis_store.py" "tests\test_api.py"`
- `rtk uvx ruff check "src\jra_srb\models.py" "src\jra_srb\analysis_store.py" "src\jra_srb\app.py" "tests\test_analysis_store.py" "tests\test_api.py"`

## 2026-07-17 14:37 自動テスト・lint確認

### 1. 実施内容

- `rtk uv run pytest -q "tests\test_analysis_store.py" "tests\test_api.py"`
- `rtk uvx ruff check "src\jra_srb\models.py" "src\jra_srb\analysis_store.py" "src\jra_srb\app.py" "tests\test_analysis_store.py" "tests\test_api.py"`

### 2. 結果

- pytest: 78 passed、1 warning、20.32秒。
- ruff: All checks passed。

### 3. 既知失敗

- なし。
- Pydantic 2.11の`BaseModel.__get_pydantic_core_schema__`非推奨警告が1件。今回の対象テストはすべて成功。

### 4. 次の確認点

- 必須確認なし。
