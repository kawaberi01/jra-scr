# 進捗サマリ

- 仕様化を開始。
- 実装方針は `AnalysisSQLiteStore` 拡張、対応表 CSV ベースの netkeiba result バッチ、指定買い目 odds の store API 追加。
- 実装完了。`analysis_store.py` に netkeiba 永続保存テーブルと保存メソッドを追加。
- `collect-netkeiba-results` CLI と対応表 CSV ベースの collector を追加。
- store/CLI テストを追加し、全体 pytest を確認済み。
- 追加確認対応として `collect-netkeiba-results --dry-run` / `--limit` を追加。
- `generate-netkeiba-mapping` を追加し、既存 `races` から対応表 CSV を生成できるようにした。
- mapping 生成は開催カレンダー CSV 指定時は確定、未指定時は `mapped_estimated` として推定出力する。
- mapping 生成の開催場解決を `races.course` 参照から `jra_race_id` の場コード復元に変更。
