# 検証メモ

## 実行結果
- `rtk .venv-win\Scripts\pytest.exe -q tests/test_analysis_store.py tests/test_cli.py`
  - 12 passed
- `rtk .venv-win\Scripts\pytest.exe -q`
  - 104 passed, 1 skipped, 1 warning
- `rtk .venv-win\Scripts\python.exe -m jra_srb.cli collect-netkeiba-results --help`
  - `--dry-run` と `--limit` が表示されることを確認
- `rtk .venv-win\Scripts\python.exe -m jra_srb.cli generate-netkeiba-mapping --help`
  - mapping 生成コマンドが表示されることを確認

## 確認内容
- netkeiba 用 SQLite テーブルが作成される。
- netkeiba result / result entries / payouts が保存される。
- netkeiba odds の指定済み entry が保存される。
- `collect-netkeiba-results` が parser で受け付けられる。
- 保存済み netkeiba result は `--refresh` なしで再取得されない。
- dry-run は live service を呼ばずに件数を返す。
- limit は mapping CSV の対象件数を先頭 N 件に制限する。
- mapping CSV は期待列を持ち、対応不能行を `mapping_status=unmapped` と理由付きで出力する。
- `races.course` が条件文字列でも、`jra_race_id` の場コードから `course` を復元して mapping できる。
