# 002-進捗サマリ

更新方式: 更新型。

## 現在地

- `JRA-NEXT-003`の仕様化、実装、自動検証が完了した。
- card取得単位のappend-only履歴と保存済みsnapshot APIのas-of合成が利用可能。

## 完了済み

- `000/005/010/020/030`。
- JRA card完全性、source種別、明示取消の抽出。
- card snapshot親子tableと旧DB additive migration。
- 完全世代間の取消導出、馬体重・騎手変更の世代保存。
- result pageと不完全cardのas-of候補除外。
- timezone付き`as_of`以下のcard/odds合成。
- 定刻オッズcollectorでのcard強制再取得。
- API文書と関連pytest。

## 未完 / 注意点

- 現fixtureにない未知のJRA取消markupは、実例入手時に明示判定を拡張する。
- 過去の最新値から履歴をbackfillしないため、履歴は本実装後のcard保存から蓄積される。
- 変更は未コミット。無関係なruntime logと一時ファイルは対象外。

## テスト状況

- 対象pytest: 114 passed、警告1件。
- 全pytest: 314 passed、1 skipped、警告1件。
- ruff: All checks passed。
- `python -m compileall -q src tests`: 成功。
- `uv build`: sdist・wheel生成成功。
- `git diff --check`: 成功。

## 次にやること

- 対象差分の確認・コミット後、共通バックログの`JRA-NEXT-004`を仕様化する。
