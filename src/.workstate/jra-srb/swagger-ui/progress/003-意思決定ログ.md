# 003-意思決定ログ

更新方式: 追記型。

## 2026-07-17 仕様化セッション

### 種別

- 分析・仕様化

### 入力

- ユーザー要件、実コード、README、テスト、FastAPI 公式資料。

### 採用判断

- FastAPI 標準 Swagger UI を利用し、新規依存を追加しない。
- 既存挙動は成立済みのため、URL 明示と `/docs` 回帰テストを実装単位とする。
- カスタム OpenAPI と既存 API メタデータを維持する。

### 変更しなかった内容

- 認証、環境別公開制御、アセット自己ホスト、テーマ、ReDoc、API 本体。

### 判断根拠

- `FastAPI(...)`、`custom_openapi()`、README の `/docs` 記載、既存 `/openapi.json` テスト。

### 影響範囲

- `jra_srb/app.py` と `../tests/test_api.py` の限定変更。

### 残件

- 実装、対象テスト、ブラウザ目視。
